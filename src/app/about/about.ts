import { Component } from '@angular/core';
import { Grid } from "../grid/grid";

@Component({
  selector: 'app-about',
  imports: [Grid],
  templateUrl: './about.html',
  styleUrl: './about.css',
})
export class About {}
